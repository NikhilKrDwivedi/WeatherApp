<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="DateFont" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">#a52307</item>
        <item name="android:textSize">25dp</item>
        <item name="android:typeface">monospace</item>
    </style>
    <style name="TextFont" parent="@android:style/TextAppearance.Widget.TextView">
        <item name="android:textColor">#079b28</item>
        <item name="android:textSize">15dp</item>
        <item name="android:typeface">monospace</item>
    </style>

</resources>
